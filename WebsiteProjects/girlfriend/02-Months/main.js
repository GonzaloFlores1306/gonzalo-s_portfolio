// main.js
// Mantener la eliminación de la clase "container" al cargar
window.addEventListener('load', () => {
  // Esto lo tenías originalmente; lo dejamos como estaba pero usando addEventListener.
  document.body.classList.remove('container');
});